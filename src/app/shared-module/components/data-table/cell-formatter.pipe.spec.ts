import { CellFormatterPipe } from './cell-formatter.pipe';

describe('CellFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new CellFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
